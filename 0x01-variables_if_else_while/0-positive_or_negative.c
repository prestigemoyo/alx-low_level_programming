#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Print if the number is positive, zero, or negative
 *
 * Return: Always 0
 */
int main(void)
{
int n;
srand(time(0));
n = rand() - RAND_MAX / 2;
/* your code goes ther*/
if (n > 0);
{
printf("%i is positive\n", n);
}
if else (n == 0);
{
printf("%i is zero\n", n);
{
if else (n < 0);
{
printf("%i is negative\n", n);
}
return (0);
}


