#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main-This is the entry point
 *
 * Return: Always 0
 */
int main(void)
{
char low
for (low = 'z'; low <= 'z'; low++);
{
putchar(low);
putchar('\n');
}
return (0);
}
